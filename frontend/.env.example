# Mapbox Public Token (Required for the map to display)
VITE_MAPBOX_TOKEN=your_mapbox_public_token_here

# Backend API URL (Automatically handled by Encore in dev, but useful for reference)
VITE_CLIENT_TARGET=http://localhost:4000
